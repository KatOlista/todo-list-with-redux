export const MAX_LENGTH = 20;

export const ERROR_MESSAGES = {
  default: '',
  emptyTitle: 'Title cannot be empty!',
  tooLongTitle: `Title cannot be longer than ${MAX_LENGTH} characters!`,
};

export const FILTER_OPTIONS = ['All', 'Completed', 'Active'];
